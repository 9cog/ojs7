# Git LFS Configuration for OJS 7.1 with SKZ Integration
#
# IMPORTANT: Do NOT track *.json via LFS — composer.json, package.json,
# schemas/*.json, and other small JSON files must remain in regular Git
# for the repository to clone and function correctly.
#
# Only track genuinely large binary files that may exceed GitHub limits.
# Text files (JSON, CSV, log, txt, md) should use .gitignore instead.

# AI Model files (genuinely large binaries)
*.model filter=lfs diff=lfs merge=lfs -text
*.h5 filter=lfs diff=lfs merge=lfs -text
*.pkl filter=lfs diff=lfs merge=lfs -text
*.onnx filter=lfs diff=lfs merge=lfs -text

# Archives and packages
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.rar filter=lfs diff=lfs merge=lfs -text

# Media files
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text
*.avi filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text

# Large images
*.psd filter=lfs diff=lfs merge=lfs -text
*.tiff filter=lfs diff=lfs merge=lfs -text
*.tif filter=lfs diff=lfs merge=lfs -text

# Large data files
*.parquet filter=lfs diff=lfs merge=lfs -text